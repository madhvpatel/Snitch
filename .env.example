# Optional. Leave unset to use same-origin routes (`/api` and `/python`)
# when the frontend is served by the Node app or shared through ngrok.
# VITE_API_BASE_URL=http://localhost:3001
# VITE_PYTHON_API_BASE_URL=http://localhost:5001
# VITE_GOOGLE_MAPS_API_KEY=your-browser-google-maps-key
